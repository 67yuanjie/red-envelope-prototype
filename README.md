# 一對一紅包功能原型

純前端、無外部依賴的可操作原型。資料只保留在目前頁面，重新整理會重設。

## 開啟方式

```powershell
node server.mjs
```

瀏覽器開啟 `http://127.0.0.1:4173`。

## 原型測試工具

- 切換小晴（發送方）與阿凱（接收方）視角
- 停用或恢復紅包功能。停用時會將待領取紅包收回並退款
- 快轉至發送後 72 小時，觸發逾期退款
- 付款測試碼固定為 `1234`

## 測試

```powershell
node --test tests/domain.test.mjs
```
